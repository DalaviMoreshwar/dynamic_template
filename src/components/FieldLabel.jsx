import React from 'react'

const FieldLabel = ({ label }) => {
  return (
    <>
      <label className="form-label text-muted mt-2 mb-0" style={{ fontSize: "14px" }}>{label}</label>
    </ >
  )
}

export default FieldLabel